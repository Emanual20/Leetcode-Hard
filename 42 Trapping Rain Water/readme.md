重要的是推出来公式，该列的贡献可以总结为如下公式：
**$$ans = \Sigma_{i=1}^{n} max(0, min(lmax[i], rmax[i]) - height[i])$$**

lmax[i]表示位置i左部最大高度，rmax[i]表示位置i右部最大高度。

推出公式就很简单了，O(n)扫两遍就行。



一开始想的比较远，想到了给定n个矩形（可能重叠），求解这些矩形所覆盖平面的面积。这个问题使用扫描线+线段树解决的，想了想怎么也套不过去。。想复杂了。