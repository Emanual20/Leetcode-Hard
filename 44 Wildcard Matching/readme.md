历经多年，我终于又被卡常了（WRNG）。

我是遍历dp数组搞的，因此需要判断一些数组下标访问是否越界。

这里分享一些卡常小技巧：

1. 尽量把不影响正确性的存储单元位宽降低，比如int数组换bool；

2. 循环展开

3. 短小的函数声明成inline，或者直接用宏替换函数做

4. 多用位运算，尽管正常编译器都会帮你做位运算优化，比如 / 2 一般都会优化成 >> 1 

   (负数不会，因为这样影响正确性了，可以思考一下原因)

5.  SIMD ? (都到这份上了，还不如重新写)